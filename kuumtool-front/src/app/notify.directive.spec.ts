import { NotifyDirective } from './notify.directive';

describe('NotifyDirective', () => {
  it('should create an instance', () => {
    const directive = new NotifyDirective();
    expect(directive).toBeTruthy();
  });
});
